# 3X-ui-ShopBot | Telegram-бот для продажи VPN

<div align="center">
    <a href="#установка-и-обновление">Установка и обновление</a> •
    <a href="#настройка">Настройка</a> •
    <a href="#поддержка">Поддержка</a>
</div>

**3X-ui-ShopBot** — это мощный инструмент для автоматизации продаж VLESS-конфигураций. Бот интегрируется с панелью **3x-ui** и предоставляет пользователям удобный интерфейс для покупки, продления и управления подписками.

## 🚀 Основные возможности

*   **Автоматизация продаж**: Выдача ключей, прием оплаты, продление подписок.
*   **Веб-панель администратора**: Удобное управление пользователями, серверами, тарифами и настройками бота.
*   **Мульти-серверность**: Поддержка множества серверов 3x-ui (балансировка и выбор локаций).
*   **Платежные шлюзы**: YooKassa, YooMoney, Unitpay, FreeKassa, CryptoBot, Telegram Stars.
*   **Реферальная система**: Настраиваемые бонусы за приглашение друзей.
*   **Тест скорости**: Встроенная функция проверки скорости соединения на серверах.
*   **Техподдержка**: Система тикетов внутри бота.
*   **Рассылки**: Отправка сообщений всем пользователям.

---

## 🛠 Установка и запуск

### Предварительные требования
*   **Python 3.10** или выше.
*   **Git**.

### Инструкция для Windows (Локальный запуск)

1.  **Склонируйте репозиторий** (если еще не сделали):
    ```powershell
    git clone https://github.com/vsvavan2/my-3xui-shopbot.git
    cd my-3xui-shopbot
    ```

2.  **Создайте виртуальное окружение**:
    ```powershell
    # Находясь в корне проекта (где лежит requirements.txt)
    python -m venv .venv
    ```

3.  **Активируйте виртуальное окружение**:
    ```powershell
    .\.venv\Scripts\Activate
    ```

4.  **Установите зависимости**:
    ```powershell
    pip install -r requirements.txt
    ```

5.  **Настройка окружения**:
    *   Убедитесь, что у вас есть файл `.env` (или создайте его на основе `.env.example`).
    *   Основные настройки (токен бота, ID админа) хранятся в базе данных `users.db`, но первичные настройки можно задать в `.env`.

6.  **Запуск бота**:
    ```powershell
    # Из корня проекта
    $env:PYTHONPATH="src"
    python -m shop_bot
    ```

### Инструкция для Ubuntu (Хостинг)

1.  **Обновите пакеты и установите Python/Git**:
    ```bash
    sudo apt update && sudo apt install python3 python3-venv python3-pip git -y
    ```

2.  **Склонируйте репозиторий**:
    ```bash
    git clone https://github.com/vsvavan2/my-3xui-shopbot.git
    cd my-3xui-shopbot
    ```

3.  **Создайте и активируйте venv**:
    ```bash
    python3 -m venv .venv
    source .venv/bin/activate
    ```

4.  **Установите зависимости**:
    ```bash
    pip install -r requirements.txt
    ```

5.  **Запуск (через systemd рекомендуется для продакшена)**:
    Создайте файл службы: `/etc/systemd/system/shopbot.service`
    ```ini
    [Unit]
    Description=3x-ui Shop Bot
    After=network.target

    [Service]
    User=root
    WorkingDirectory=/path/to/my-3xui-shopbot
    Environment="PYTHONPATH=/path/to/my-3xui-shopbot/src"
    ExecStart=/path/to/my-3xui-shopbot/.venv/bin/python -m shop_bot
    Restart=always

    [Install]
    WantedBy=multi-user.target
    ```
    Затем: `sudo systemctl enable --now shopbot`

---

## ⚙️ Настройка и Администрирование

### Веб-панель
После запуска бота админ-панель доступна по адресу: `http://localhost:1488` (или IP вашего сервера).
*   **Логин/Пароль по умолчанию**: `admin` / `admin` (измените в настройках!).

### Управление контентом (Тексты бота)
Все тексты кнопок, приветствий и сообщений можно изменить в админ-панели:
1.  Перейдите в раздел **Настройки** -> **Контент**.
2.  Найдите нужный блок (например, "Главное меню", "Реферальная программа").
3.  Отредактируйте текст (поддерживается HTML) и нажмите **Сохранить**.
4.  Изменения вступают в силу мгновенно.

### Настройка реферальной программы
1.  В админ-панели перейдите в **Настройки**.
2.  Найдите раздел **Реферальная система**.
3.  Вы можете изменить текст приглашения ("Приглашайте друзей...") в разделе **Контент**.
4.  Ссылка для приглашения формируется автоматически: `https://t.me/YourBot?start=ref_ID`.

### Настройка Speedtest (Тест скорости)
Чтобы кнопка "⚡ Тест скорости" работала:
1.  В админ-панели добавьте хосты (серверы 3x-ui).
2.  Укажите данные для SSH подключения к этим серверам.
3.  Нажмите кнопку **"Автоустановка Speedtest"** (в разделе хостов или через админ-меню бота), чтобы установить необходимые утилиты на удаленные серверы.
4.  Если пользователи видят ошибку, проверьте логи бота и доступность SSH.

### Бэкапы
Бот автоматически создает резервные копии базы данных `users.db`.
*   Настройка интервала бэкапов доступна в админ-панели.
*   Чтобы очистить старые бэкапы, используйте скрипт `cleanup_backups.py` или удалите файлы из папки `backups/`.

---

## 📞 Поддержка

Если у вас возникли вопросы, проверьте логи в консоли.
Ошибки вида "Функция в разработке" обычно означают, что соответствующая настройка не найдена в базе данных или отключена. Проверьте раздел **Контент** в админ-панели.
