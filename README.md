# 🤖 Telegram Bot для продажи VLESS VPN (3x-ui)

Мощный и функциональный бот для автоматической продажи VPN-ключей, работающий в связке с панелью 3x-ui (XRAY). Включает в себя веб-админку, поддержку множества платежных систем, реферальную программу и систему технической поддержки.

## ✨ Основные возможности

### 🛒 Продажа и Управление
*   **Автоматическая выдача ключей**: VLESS ключи создаются мгновенно после оплаты.
*   **Мульти-серверность**: Поддержка нескольких серверов 3x-ui.
*   **Личный кабинет**: Просмотр купленных ключей, продление, инструкции по подключению (Android, iOS, Windows, Linux).
*   **Тест скорости**: Отображение загруженности серверов (ping, upload/download) для пользователей.
*   **Пробный период**: Настраиваемый триал для новых пользователей.

### 💳 Платежные системы
*   **ЮMoney (YooMoney)**: Прямая интеграция (P2P и OAuth) и через HTTP-уведомления.
*   **Telegram Stars**: Прием оплаты внутренней валютой Telegram.
*   **YooKassa, UnitPay, FreeKassa, Enot.io**: Поддержка популярных агрегаторов.
*   **Криптовалюты**: Через CryptoBot (опционально).

### 👥 Маркетинг и Лояльность
*   **Реферальная программа**: Начисления % от покупок приглашенных друзей.
*   **Промокоды**: Скидки (процентные или фиксированные) и бонусные дни.
*   **Рассылки**: Отправка сообщений всем пользователям через админку.

### ⚙️ Админ-панель (Web UI)
*   Удобный веб-интерфейс по адресу `http://ваш-ip:1488`.
*   Управление пользователями (баланс, блокировка, просмотр покупок).
*   Управление ключами и серверами.
*   Настройка тарифов и цен.
*   Обработка тикетов поддержки.
*   Просмотр статистики и логов.

---

## 🚀 Установка и Запуск (Docker) - Рекомендуется

Это самый простой способ запуска на сервере (Ubuntu/Debian).

### 1. Подготовка
Убедитесь, что установлены `docker` и `docker-compose`.

```bash
sudo apt update
sudo apt install docker.io docker-compose -y
```

### 2. Загрузка проекта
Склонируйте репозиторий:

```bash
git clone https://github.com/ваш-юзернейм/3xui-shopbot.git
cd 3xui-shopbot/3xui-shopbot-main
```

### 3. Настройка
1.  Отредактируйте `docker-compose.yml` если нужно сменить порты.
2.  Настройте Nginx (файл `nginx_vless.conf`) для работы по HTTPS (см. раздел Nginx).

### 4. Запуск
```bash
sudo docker-compose up -d --build
```

После запуска:
*   **Бот**: Начнет отвечать в Telegram.
*   **Админка**: Доступна по адресу `http://ip-сервера:1488`.
    *   **Логин**: `admin`
    *   **Пароль**: `admin` (Смените в настройках!)

### 🔄 Обновление
Чтобы обновить проект без ошибок:

```bash
# Остановка и очистка старых контейнеров
sudo docker-compose down --rmi all --remove-orphans

# Получение свежего кода
git pull

# Запуск
sudo docker-compose up -d --build
```

---

## 🛠 Ручная установка (для разработки)

Если вы хотите запустить проект локально или без Docker.

1.  **Python**: Требуется Python 3.10+.
2.  **Виртуальное окружение**:
    ```bash
    python3 -m venv .venv
    source .venv/bin/activate  # Linux/Mac
    .venv\Scripts\Activate     # Windows
    ```
3.  **Зависимости**:
    ```bash
    pip install -r requirements.txt
    ```
4.  **Запуск**:
    ```bash
    python -m src.shop_bot
    ```

---

## 🌐 Настройка Nginx (HTTPS)

Для корректной работы вебхуков (YooMoney) и безопасного доступа к админке рекомендуется использовать Nginx с SSL.

Пример конфига (`/etc/nginx/sites-available/default` или `vless.conf`):

```nginx
server {
    listen 80;
    server_name vless.24x7.hk;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name vless.24x7.hk;

    ssl_certificate /etc/letsencrypt/live/vless.24x7.hk/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vless.24x7.hk/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:1488;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

## 💰 Настройка ЮMoney (YooMoney)

Для работы автоматической оплаты (P2P) и проверки платежей требуется настроить **два разных компонента**. Не путайте их!

### 1. Настройка HTTP-уведомлений (Webhook)
*Отвечает за мгновенное зачисление средств при поступлении перевода.*

1.  Перейдите по ссылке: [https://yoomoney.ru/transfer/myservices/http-notification?lang=ru](https://yoomoney.ru/transfer/myservices/http-notification?lang=ru)
2.  **Куда отправлять (URL)**: Введите адрес вашего бота с окончанием `/yoomoney-webhook`.
    *   Пример: `https://vless.24x7.hk/yoomoney-webhook`
3.  ✅ Поставьте галочку **"Отправлять уведомления"**.
4.  Нажмите кнопку **"Показать секрет"**.
5.  **СКОПИРУЙТЕ ЭТОТ СЕКРЕТ**. Он короткий (около 24 символов).
    *   Вставьте его в настройках бота в поле **"Секретное слово (для HTTP-уведомлений)"**.

### 2. Настройка Приложения (OAuth)
*Отвечает за проверку истории операций и автоматическое подтверждение.*

1.  Чтобы создать новое приложение, перейдите: [https://yoomoney.ru/myservices/new](https://yoomoney.ru/myservices/new)
2.  Заполните форму:
    *   **Название**: `Vless Bot` (или любое другое).
    *   **Адрес сайта**: Ваш домен (например, `https://vless.24x7.hk`).
    *   **Redirect URI**: Ваш домен + `/yoomoney/callback` (например, `https://vless.24x7.hk/yoomoney/callback`).
    *   ✅ **Проверять подлинность приложения**: Обязательно поставьте галочку.
3.  Нажмите **"Создать"**.
4.  Вы получите `Client ID`. Скопируйте его в настройки бота в поле **"YooMoney client_id"**.
5.  (Опционально) Если нужен Client Secret, он там же.
6.  Чтобы посмотреть список ваших приложений, используйте ссылку: [https://yoomoney.ru/settings/oauth-services](https://yoomoney.ru/settings/oauth-services)

### 3. Завершение настройки в Админке
1.  Перейдите в админку бота -> **Настройки** -> **Платежи**.
2.  Вставьте **Секрет уведомлений** (из шага 1) в поле "Секретное слово".
3.  Вставьте **Client ID** (из шага 2).
4.  Нажмите **Сохранить**.
5.  Нажмите кнопку **"Подключить YooMoney (OAuth)"** и подтвердите доступ.

Теперь платежи будут зачисляться автоматически.

---

## 🆘 Решение проблем

### 1. Ошибка обновления Git: `users.db`
Эта ошибка означает, что ваша локальная база данных отличается от той, что в репозитории.
**Решение:** Выполните процедуру обновления из раздела **"🔄 Обновление"** (выше). Она включает шаги с бэкапом и восстановлением базы.

### 2. Ошибка 404 на `/yoomoney/callback`
Это значит, что сервер работает на старой версии кода. Выполните инструкцию из пункта 1 (обновление Git) и перезапустите Docker:
```bash
sudo docker-compose down --rmi all --remove-orphans
sudo docker-compose up -d --build
```

### 3. Ошибка `ContainerConfig` в Docker
Если Docker выдает странные ошибки при запуске, выполните полную очистку:
```bash
sudo docker-compose down --rmi all --remove-orphans
sudo docker-compose up -d --build
```

### 4. Ошибка SSL (Незащищенное соединение)
Если сайт открывается с ошибкой "Подключение не защищено" или сертификат от Google (`NET::ERR_CERT_COMMON_NAME_INVALID`):

1.  **Установите Certbot**:
    ```bash
    sudo apt update
    sudo apt install -y certbot python3-certbot-nginx
    ```
2.  **Получите сертификат** (команды для вашего домена):
    ```bash
    sudo systemctl stop nginx
    sudo certbot certonly --standalone -d vless.24x7.hk --non-interactive --agree-tos -m inet25@bk.ru
    sudo systemctl start nginx
    ```

